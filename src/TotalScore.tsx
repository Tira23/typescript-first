import React, { useContext, useEffect, useState } from "react";
import { Context } from "./Context";

export const TotalScore = () => {
    const { score, user, time } = useContext(Context);

    return <div></div>;
};
